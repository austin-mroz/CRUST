[package]
name = "crust"
version = "0.0.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "crust"
create-type = ["cdylib"]

[profile.release]
lto = true
opt-level = 3
codegen-units = 1
